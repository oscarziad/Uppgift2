<Page
    x:Class="Uppgift2.ViewModels.AllClosedView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Uppgift2.ViewModels"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}" d:DesignWidth="1200" d:DesignHeight="1000">

    <Grid Margin="100">
        <Grid.RowDefinitions>
            <RowDefinition Height="100"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBox Grid.Row="0" Text="All Closed Tickets" HorizontalAlignment="Left" FontSize="30" FontWeight="Bold" BorderBrush="Transparent"/>
        <ListView x:Name="lvClosedTickets" Grid.Row="1">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <TextBlock />
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

    </Grid>
</Page>
